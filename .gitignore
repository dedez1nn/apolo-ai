# Python
__pycache__/
*.pyc
*.pyo
.venv/

# Build do lançador Windows (windows/build.bat)
windows/.buildvenv/
windows/build/
windows/dist/
windows/*.spec

# Build do lançador macOS (macos/build.sh)
macos/.buildvenv/
macos/build/
macos/dist/
macos/apolo.iconset/
macos/apolo.icns

# Caches de ferramentas
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Segredos e estado local
.env
*.db
*.db-wal
*.db-shm

# Escritas atômicas (.env / config.toml gravam num *.tmp antes do replace)
*.tmp
